<Window x:Class="NovelReader.OpenBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NovelReader"
        mc:Ignorable="d"
        Title="Open Book" Height="450" Width="800">
    <ScrollViewer>
        <ItemsControl x:Name="CardItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#ddd" BorderThickness="1" Margin="10" CornerRadius="5" Padding="10" Background="White">
                        <StackPanel>
                            <Image Source="{Binding ImageBase64, Converter={StaticResource Base64ToImageConverter}}" Width="100" Height="100" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding BookName}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding Author}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Window>
